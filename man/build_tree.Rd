% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylo.R
\name{build_tree}
\alias{build_tree}
\title{Build the tree the distance metrics imply}
\usage{
build_tree(
  alignment,
  model = NULL,
  metric = NULL,
  root = c("midpoint", "outgroup", "none"),
  outgroup = NULL
)
}
\arguments{
\item{alignment}{A `vs_alignment`.}

\item{model}{Substitution model, or a `vs_model_selection` object.}

\item{metric}{Distance metric used to build the tree; see
[distance_metrics()].}

\item{root}{`"best"` (the root that minimises the regression's residual
variance), `"midpoint"`, `"outgroup"` (requires `outgroup`) or `"none"`
(use the tree as returned by neighbour joining).}

\item{outgroup}{Tip labels forming the outgroup when `root = "outgroup"`.
The outgroup is dropped from the regression after rooting.}
}
\value{
A `phylo` object whose tip labels are sequence ids.
}
\description{
Neighbour joining on the same distance matrix the rest of the package uses,
so the tree, the divergence values and the rate estimate are all derived from
one set of assumptions rather than three.
}
