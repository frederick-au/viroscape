% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve-sequences.R
\name{fetch_sequences}
\alias{fetch_sequences}
\title{Retrieve influenza sequences from NCBI}
\usage{
fetch_sequences(
  countries,
  years = c(2003, 2022),
  subtype = "H5",
  segment = "hemagglutinin",
  molecule = c("protein", "nucleotide"),
  max_records = 5000,
  cache_dir = vs_cache_dir(),
  refresh = FALSE,
  api_key = Sys.getenv("ENTREZ_KEY", ""),
  strict = TRUE,
  host_map = NULL
)
}
\arguments{
\item{countries}{Character vector of country names, or `NULL` for no country
term at all.}

\item{years}{Length-2 integer vector giving the inclusive *collection* year
range. Only the lower bound enters the query.}

\item{subtype}{Haemagglutinin subtype, e.g. `"H5"` for all H5Nx, or a full
subtype such as `"H5N1"`.}

\item{segment}{Gene segment name, e.g. `"hemagglutinin"`.}

\item{molecule}{`"protein"` or `"nucleotide"`.}

\item{max_records}{Maximum number of records to download.}

\item{cache_dir}{Cache directory; see [vs_cache_dir()].}

\item{refresh}{Force a fresh download.}

\item{api_key}{Optional NCBI API key (raises the rate limit).}

\item{strict}{Apply the exact post-download filters. `FALSE` returns
everything the query matched, which is useful for auditing what the recall
filter actually pulled in.}

\item{host_map}{Passed to [classify_host()].}
}
\value{
A `vs_sequences` object: a list with `seqs` (an `AAbin` or `DNAbin`
  list), `meta` (a tibble of per-sequence metadata) and `query`.
}
\description{
Live retrieval with on-disk caching. Requires the `rentrez` package.
}
\details{
GenBank/GenPept flat records are downloaded rather than FASTA, because
country, collection date and host are `source` feature qualifiers and are not
present in a defline. The Entrez query is a recall filter only (see
[build_entrez_query()]); the requested countries, collection years and
subtype are then applied exactly to the parsed qualifiers, and the number of
records each filter removes is reported.
}
