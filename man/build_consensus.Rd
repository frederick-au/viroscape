% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensus.R
\name{build_consensus}
\alias{build_consensus}
\title{Build a consensus sequence from an alignment}
\usage{
build_consensus(
  alignment,
  method = c("majority", "threshold"),
  threshold = 0.5,
  ambiguity = NULL,
  subset = NULL,
  ignore_gaps = TRUE
)
}
\arguments{
\item{alignment}{A `vs_alignment` object.}

\item{method}{`"majority"` takes the most common non-gap character at each
site; `"threshold"` requires the plurality character to reach `threshold`
and emits `ambiguity` otherwise.}

\item{threshold}{Proportion required by the `"threshold"` method.}

\item{ambiguity}{Character emitted where no residue meets the threshold.}

\item{subset}{Optional logical vector or sequence ids to build from.}

\item{ignore_gaps}{Exclude gaps when counting characters at each site.}
}
\value{
A `vs_consensus` object: a character vector of the consensus with
  per-site support attached.
}
\description{
The response variable in this framework is divergence from a regional
consensus, so the consensus is the reference point the whole analysis hangs
on. By default it is built from every sequence in the alignment by simple
majority rule, matching the published method. Building it from a subset (for
example wild bird isolates only) is often more interpretable, which is what
`subset` is for.
}
