% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join.R
\name{join_environment}
\alias{join_environment}
\title{Join divergence estimates to environmental predictors}
\usage{
join_environment(
  distances,
  environment,
  by = c("country", "year"),
  scale = TRUE,
  drop_incomplete = TRUE,
  cluster = by
)
}
\arguments{
\item{distances}{A `vs_distances` tibble from [distance_from_consensus()].}

\item{environment}{A country-year (or country-admin1-year) tibble.}

\item{by}{Join keys.}

\item{scale}{Standardise numeric predictors so coefficients are comparable.
Coefficients are then per standard deviation, not per natural unit; the
centre and scale are kept and reported by [tidy_coefficients()], and
[unscale()] converts back.}

\item{drop_incomplete}{Drop rows with missing predictor values, which model
comparison by AIC requires: models fitted to different numbers of rows are
not comparable.}

\item{cluster}{Columns defining the level at which the predictors actually
vary. Defaults to the join keys, which is almost always right: an
environmental indicator is a country-year quantity, and every sequence from
that country-year carries the same value. Sequence-level inference ignores
this and is badly anti-conservative; see [cluster_summary()] and
[aggregate_to_cluster()].}
}
\value{
A `vs_dataset` object.
}
\description{
Join divergence estimates to environmental predictors
}
